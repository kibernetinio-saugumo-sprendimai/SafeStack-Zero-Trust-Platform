# SafeStack Certificate Manager

This component is part of the SafeStack Zero Trust Platform. It provides a local-first foundation with explicit operator control and no automatic external actions. See `zt/extensions.py` and the platform threat model before deployment.
